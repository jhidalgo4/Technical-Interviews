# -*- coding: utf-8 -*-

from Heap import Heap
import heapq

if __name__ == "__main__":
        
    # h = Heap()
    
    # h.insert(45)
    # h.insert(90)
    # h.insert(20)
    # h.insert(50)
    # h.insert(37)
    # h.insert(39)
    # h.display()
    
    # print( h.parent(0) )
    
    
    hq = []
    heapq.heappush(hq, 12)
    heapq.heappush(hq, 79)
    heapq.heappush(hq, 20)
    heapq.heappush(hq, 80)
    print(hq)
    
    
    